/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "img2.pic.in.th",
            "img5.pic.in.th",
            "res.cloudinary.com"
        ],
    }
};

export default nextConfig;
